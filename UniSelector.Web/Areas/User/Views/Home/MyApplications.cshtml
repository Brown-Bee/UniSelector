@* @model IEnumerable<StudentRequest>

<div class="container mt-5">
    <div class="card shadow">
        <div class="card-header bg-primary text-white">
            <h2 class="text-center">My Applications</h2>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>University</th>
                            <th>Faculty</th>
                            <th>Submission Date</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var app in Model)
                        {
                            <tr>
                                <td>@app.University.Name</td>
                                <td>@app.Faculty.StandardFaculty.CombinedName</td>
                                <td>@app.SubmissionDate.ToShortDateString()</td>
                                <td>
                                    <span class="badge bg-@(app.Status switch {
                                        RequestStatus.Pending => "warning",
                                        RequestStatus.Accepted => "success",
                                        RequestStatus.Rejected => "danger",
                                        _ => "secondary"
                                    })">
                                        @app.Status
                                    </span>
                                </td>
                                <td>
                                    <a asp-action="ApplicationDetails" asp-route-id="@app.Id"
                                       class="btn btn-sm btn-primary">
                                        View Details
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div> *@